// calcular area de un triangulo

const base = parseFloat (prompt("Por favor ingrese la base del triangulo:"));
const altura = parseFloat (prompt("Por favor ingrese la altura del triangulo:"));

area = ((base * altura) / 2)

alert (`El area del triangulo corresponde a ${area}`);
