//Lanzar el cuadrado de un número ingresado

const numero = parseFloat(prompt("Ingrese un número para calcular su cuadrado"));

cuadrado = (numero * numero);

alert (`El cuadrado del número ingresado es ${cuadrado}`);